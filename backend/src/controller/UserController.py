"""from backend.src.app import db
from src.model.models import User
from datetime import datetime
from flask import Flask, jsonify

def get_user():
    user = User(name = "juan", lastname="perez", email="juan@gmail.com", date=datetime.now())
    db.session.add(user)
    db.session.commit()"""
    


